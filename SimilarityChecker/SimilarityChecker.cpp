#include <string>

using namespace std;

class SimilarityChecker
{
public:
	int getLengthPoint(string input1, string input2)
	{
		return MAX_POINT_LENGTH;
	}

	enum MAX_POINT {
		MAX_POINT_LENGTH = 60,
	};
};